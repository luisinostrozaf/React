import { useState } from 'react'

const receta = () => {
    const [recipes, setRecipes] = useState([])
    
    return (
        <div>
        <h1>RECIPIFY</h1>
        </div>
    )
};

export default receta;